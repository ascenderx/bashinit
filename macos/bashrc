# See .bash_profile for Bash configuration.
# This file should only be read via XTerm initialization.
if [ "$SSH_TTY" ]
then
    source .bash_profile
fi

if [ "$TERM" = "xterm-color" ]
then
    source .bash_profile
fi
